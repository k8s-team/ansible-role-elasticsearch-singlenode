---
# defaults file for elasticsearch

destFolder: "{{ ansible_env.HOME }}/.k8s-team/elasticsearch-singlenode"
state: present

releaseName:  elasticsearch-singlenode-chart

persistence:
  enabled: false
  volumeClaim: volumeClaim

ingress:
  enabled: false
  host: example.com
